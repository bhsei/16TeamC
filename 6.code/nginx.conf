user www nogroup;
worker_processes 3;
pid /home/sensetime/soft/16TeamC/6.code;
error_log /home/sensetime/soft/16TeamC/6.code debug;
worker_rlimit_nofile 1024;
events {
	use select;
	worker_connections 6;
}
http {
	server {
		listen 80;
		server_name www.example.com ;
		index index.html index.htm ;
		root /var/www;
	}
}
